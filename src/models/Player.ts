export interface Player {
    name: string;
    playerPosition: string;
    team: string;
    score: number;
}

export interface PlayerDTO {
    name: string;
    position: string;
    team: string;
    score: number;
}
